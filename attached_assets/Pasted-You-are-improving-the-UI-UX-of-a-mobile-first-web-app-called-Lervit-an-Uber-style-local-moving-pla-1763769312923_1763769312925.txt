You are improving the UI/UX of a mobile-first web app called Lervit – an Uber-style local moving platform. The current interface is not fully optimized for mobile, lacks competitive visual quality, spacing consistency, responsiveness, and clarity. 

TASK: 
Redesign and refine the mobile view to meet high industry standards comparable to Uber, Bolt, Lyft, DoorDash, and TaskRabbit. Ensure that the layout is modern, clean, touch-friendly, and professionally aligned. 

REQUIREMENTS:
1. Use responsive design principles (flexbox, grid, % sizing, min/max width, media queries).
2. Ensure proper padding, spacing, font scaling, button sizing, and touch targets (44px+).
3. Remove clutter. Use whitespace intelligently for a clean professional look.
4. Improve color scheme and visual hierarchy using brand accent colors (orange: #FF6A00, deep teal: #004C4C, white, and light gray).
5. Buttons must be bold, full-width, rounded, with high contrast CTA (Book Move, Confirm Order).
6. Use card-style components with soft shadows and rounded corners to display bookings, pricing, mover details, stairs/manpower options.
7. Header should have clear branding, clean navigation (back arrow, profile icon, help icon).
8. Use clear input fields with labels, icons (map pin for pickup, drop-off), and placeholder texts.
9. Add fixed bottom CTA sections for actions like “Get Estimate”, “Request Driver”, "Confirm Booking".
10. Ensure usability: No horizontal scrolling, no overlapping elements, no clipped dropdowns.
11. Make animations subtle but smooth (hover, loading, transition between pages).

Include:
✔ CSS media queries for screens < 430px width.
✔ Standard mobile breakpoints (375px, 414px, 430px).
✔ Responsive layout adjustments for iPhone, Android, foldable devices.

Output the improved:
- HTML structure (if needed)
- CSS improvements and media queries
- React components (if applicable)
- Suggested UI changes with clean formatting

Make the interface feel professionally built, touch-friendly, and visually premium. 
