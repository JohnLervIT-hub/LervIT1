You are helping me upgrade my current image-analysis system into **LervIT Vision Engine 2.0**.

Goal: Make the system produce stable, consistent, and accurate predictions for furniture dimensions, volume (in cubic feet), weight, load-size classification, and recommended vehicle. It must avoid giving very different outputs for the same item and aim for >95–99% consistency.

Please design and describe the architecture, logic, and example code for this upgraded Vision Engine, following ALL requirements below.

OBJECTIVE
- Correctly identify the furniture item from a photo
- Estimate or retrieve dimensions (L × W × H)
- Calculate volume in cubic feet
- Estimate realistic weight
- Assign the correct load-size category
- Recommend the correct vehicle type
- Produce consistent, repeatable results for the same item
- Avoid large fluctuations between scans of the same furniture

CORE REQUIREMENT – THREE-LAYER SYSTEM

LAYER 1: Ground-Truth Furniture Database
Create a structured furniture dataset that contains, for each item:
- item_id
- name
- category (e.g. Bed, Sofa, Table, Appliance, etc.)
- subcategory (e.g. Twin, Queen, Sectional, Fridge, etc.)
- a list of example images
- exact real dimensions in cm or inches (length, width, height)
- computed volume in ft³
- weight in kg
- load_size (e.g. Boxes, Medium, Large, Apartment)
- recommended vehicle (e.g. Cargo Van, Pickup Truck, Moving Truck)
- an embedding vector for similarity search

Example JSON schema (just as a reference for structure):

{
  "item_id": "BED_TWIN_STORAGE_001",
  "name": "Twin bed frame with storage drawers",
  "category": "Bed",
  "subcategory": "Twin",
  "images": ["img1.jpg", "img2.jpg"],
  "dimensions_cm": { "length": 191, "width": 99, "height": 43 },
  "volume_ft3": 41.5,
  "weight_kg": 120,
  "load_size": "Medium",
  "vehicle": "Cargo Van",
  "embedding": "VECTOR_PLACEHOLDER"
}

You don’t need to generate real vectors, just show how the schema and logic should work.

LAYER 2: Embedding Similarity Matching (Stabilization Layer)
The Vision Engine should first try to match the uploaded photo against the furniture database using embeddings, to get stable outputs for common items.

Logic:
1. User uploads a photo.
2. Generate an image embedding for the uploaded photo.
3. Compare it to all item embeddings in the furniture database.
4. If the best match similarity score is > 0.90 (or another clearly defined threshold):
   - Use the matched item’s stored dimensions, volume, weight, load_size, and vehicle.
   - Do NOT estimate dimensions from scratch.
5. If the similarity is below the threshold:
   - Fall back to dimension estimation using the vision model.

Please provide pseudocode for this similarity step, something like:

uploaded_embedding = get_embedding(uploaded_photo)
match = find_best_match(uploaded_embedding, furniture_database)

if match.similarity > 0.90:
    use match.dimensions, match.volume, match.weight, match.load_size, match.vehicle
else:
    run dimension_estimation(uploaded_photo)

LAYER 3: Dimension Correction Rules (Normalize Predictions)
When we do have to estimate dimensions (because similarity < threshold), apply category-specific correction rules to keep dimensions realistic and avoid crazy values caused by camera angle or distortion.

For example:
- For beds:
  - length between 72 and 85 inches
  - width between 36 and 78 inches (twin to king range)
  - height between 10 and 60 inches
- For sofas:
  - length between 60 and 140 inches
  - depth between 30 and 45 inches
  - height between 28 and 40 inches
- For fridges:
  - height between 60 and 75 inches
  - width between 28 and 36 inches
  - depth in a realistic range

Show this as generic “clamp” logic in pseudocode, like:

if category == "Bed":
    length = clamp(predicted_length, min_bed_length, max_bed_length)
    width  = clamp(predicted_width,  min_bed_width,  max_bed_width)
    height = clamp(predicted_height, min_bed_height, max_bed_height)

Do this for a few main categories (Bed, Sofa, Table, Appliance, etc.).

FULL PIPELINE – LERVIT VISION ENGINE 2.0

Please define the full end-to-end pipeline like this:

1. Image Upload:
   - User uploads a photo.
   - Preprocess/normalize the image.

2. Category Detection:
   - Detect the furniture category and subcategory from the image (e.g. “Twin bed”, “Sectional sofa”, “Fridge”).

3. Embedding Generation:
   - Generate an embedding vector for the uploaded image.

4. Similarity Search:
   - Compare the embedding with the furniture database embeddings.
   - If similarity > threshold (e.g. 0.90), reuse ground-truth data.
   - Otherwise, continue to estimation.

5. Dimension Estimation:
   - Use the vision model to estimate length, width, and height.

6. Dimension Correction:
   - Apply category-specific clamp rules so dimensions fall within realistic ranges.

7. Volume Calculation:
   - Convert corrected dimensions into cubic feet.

8. Weight Estimation:
   - Estimate weight based on category + dimensions using typical ranges or density assumptions.

9. Load Size Classification:
   - Use a volume-based rule, for example:
     - Boxes: 1–10 ft³
     - Medium: 11–50 ft³
     - Large: 51–170 ft³
     - Apartment: 171+ ft³

10. Vehicle Recommendation:
   - Map load_size (and possibly weight/dimensions) to a vehicle type:
     - Boxes/Medium: Cargo Van
     - Large: Pickup or Moving Truck
     - Apartment: Moving Truck

11. Final Output:
   - Return a structured JSON-like result containing:
     - item_name
     - category
     - dimensions
     - volume_ft3
     - weight_kg
     - load_size
     - vehicle
     - whether the result came from database match or from estimation
     - a confidence or accuracy score

DELIVERABLES I WANT FROM YOU
Please respond with:

1. A clear description of the full LervIT Vision Engine 2.0 architecture.
2. The recommended JSON schema for the furniture database.
3. Pseudocode for the embedding similarity search logic.
4. Example dimension-correction rules for at least Beds, Sofas, Tables, and Fridges.
5. Pseudocode for the full pipeline from image upload to final JSON result.
6. Example outputs for:
   - a twin bed with storage,
   - a 3-seater sofa,
   - a dining table with 4 chairs,
   - and a fridge.
7. Implementation notes and suggestions for using this in a Replit project (Node.js or Python), including where the model calls, database, and similarity search would live in the codebase.

Follow these instructions carefully and structure the answer in a way that a developer can implement step by step.
