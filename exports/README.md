# exports/ — Internal Documents

These files are internal only.

The `/api/downloads/*` endpoints now require admin authentication.
Unauthenticated or non-admin requests receive a 403 response.

Do not distribute or commit changes to these files without approval from engineering leadership.
